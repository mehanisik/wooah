/**
 * Sync indicator is no longer needed — Convex handles real-time sync automatically.
 * This file is kept as a no-op export so existing imports don't break.
 */
export function SyncIndicator() {
  return null
}
